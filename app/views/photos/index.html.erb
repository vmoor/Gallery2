<h1>Listing photos</h1>

<table>
  <tr>
    <th>Album</th>
    <th>Image</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @photos.each do |photo| %>
  <tr data-photo-id=<%= photo.id %>>
    <td><%= photo.album_id %></td>
    <td><%= image_tag(photo.image.thumb.url) if photo.image? %></td>
    <td><%#= link_to 'Show', photo %></td>
    <td><%= link_to 'Edit', edit_album_photo_path(@album, photo) %></td>
    <td><%= link_to 'Destroy', album_photo_path(@album, photo, :json), "data-photo-destroy-id" => photo.id %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Photo', new_album_photo_path(@album) %>
